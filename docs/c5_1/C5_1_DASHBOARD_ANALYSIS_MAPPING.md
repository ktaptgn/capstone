# Dashboard Analysis Mapping

## Overview Cards

- `total_cost` -> Total Cost
- `demand_fulfillment_rate` -> Demand Fulfillment
- `average_available_trucks` -> Available Trucks
- `pm_cost` -> PM Cost
- `unmet_demand` -> Unmet Demand
- `queue_time` -> Queue Time

## Policy Comparison

Use `policy_kpi_summary.csv` and `policy_ranking_table.csv` from `outputs/c5_1/analysis/`.

## Recommended Policy Badge

Use `composite_score` only as a display helper. Do not label it as a mathematical optimum.

## Drill-down

- H0 improvement table -> baseline comparison section
- Stability table -> seed robustness note
- Trade-off notes -> dashboard explanation panel
